export interface Experience {
  id: number;
  titre: string;
  entreprise: string;
  local: string;
  datedebut: string;
  datefin: string;
  descriotion: string;
  responsabilite: string;
  locale: string;
}